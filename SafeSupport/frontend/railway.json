{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx vite preview --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "environments": {
    "production": {
      "variables": {
        "PORT": "8080"
      }
    }
  }
}
